# Server-only requirements (no Pygame or heavy dependencies)
# The server only uses Python standard library modules
# No external dependencies needed - pure Python sockets

# If you need to add any future dependencies, list them below:
# example: requests==2.31.0
